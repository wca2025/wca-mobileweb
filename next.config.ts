import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Add your CodeSandbox preview domain here
  allowedDevOrigins: ["dkny5l-3000.csb.app"],

  /* other config options if needed */
};

export default nextConfig;
